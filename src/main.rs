use crate::vm::VM;

mod instructions;
mod memory;
mod register_types;
mod registerfile;
mod vm;

fn main() {
    // let mut vm = VM::new();

    // TODO Actually run the code

    // TODO Add a file reader
}
