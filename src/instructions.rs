enum Instruction {
    AD(usize),
    ADS(usize),
    AUG(usize),
    BZF(usize),
    BZMF(usize),
    CA(usize),
    CCS(usize),
    COM, // WHAT DO YOU MEAN: compiles to CS A, what do I do :sob:
    CS(usize),
    DAS(usize),
    DCA(usize),
    DCOM,
    DCS(usize),
    DDOUBL,
    DIM(usize),
    DOUBLE,
    DTCB,
    DTCF,
    DV(usize),
    DXCH(usize),
    EXTEND,
    INCR(usize),
    INDEX(usize),
    INHINT,
    LXCH(usize),
    MASK(usize),
    MP(usize),
    MSU(usize),
    NOOP,
    OVSK,
    QXCH(usize),
    RAND(usize),
    READ(usize),
    RELINT,
    RESUME,
    RETURN,
    ROR(usize),
    RXOR(usize),
    SQUARE,
    SU(usize),
    TC(usize),
    TCAA,
    TCF(usize),
    TS(usize),
    WAND(usize),
    WOR(usize),
    WRITE(usize),
    XCH(usize),
    XLQ,
    XXALQ,
    ZL,
    ZQ,
}
